# never upload dependencies
node_modules

# build outputs
dist
build

# env / secrets
.env
*.env
*.env.*
!*.env.example

# runtime data (local/ephemeral)
server/data/*.db
server/data/backups/*.db
server/uploads/
server/logs/*.log

# mac junk
.DS_Store
